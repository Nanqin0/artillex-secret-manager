# --- Server bind ---
HOST=127.0.0.1
PORT=8000

# --- Audit ---
AUDIT_ENABLED=true
AUDIT_TTL_DAYS=0

# --- Secrets / DB ---
# 32-byte random key, base64-encoded (REQUIRED). Do NOT commit the real value.
MASTER_KEY=<<paste_32_byte_base64_here>>

# Local dev on host:
MONGO_URI=mongodb://localhost:27017
# If using docker-compose/swarm inside containers, use:
# MONGO_URI=mongodb://mongo:27017
